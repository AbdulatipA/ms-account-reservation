services:
  postgres-db:
    image: postgres:17
    container_name: account_reservation_db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: account_reservation
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: